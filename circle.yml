machine:
  services:
    - docker

dependencies:
  override:
    - bundle install
    - ./docker.sh login
    - docker info
    - ./docker.sh pull 6.5.2

test:
  override:
    - ./docker.sh run 6.5.2; sleep 10
    - SPLUNK_VERSION=6.5.2 bundle exec rake coverage
